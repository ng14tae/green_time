<div class="mb-6 sm:mb-8">
    <div class="flex justify-center gap-2 sm:gap-3">
        <!-- ホーム -->
        <%= link_to root_path,
            class: "flex flex-col items-center bg-white rounded-lg shadow p-3 sm:p-4 hover:shadow-md transition-all w-20 sm:w-24" do %>
        <div class="text-2xl sm:text-3xl mb-1">🏠</div>
        <span class="text-xs font-semibold text-gray-700">ホーム</span>
        <% end %>

        <!-- チェックイン/アウト -->
        <% if today_record %>
        <%= link_to checkout_page_checkinout_records_path,
            class: "flex flex-col items-center bg-red-500 rounded-lg shadow p-3 sm:p-4 hover:shadow-md transition-all w-20 sm:w-24" do %>
            <div class="text-2xl sm:text-3xl mb-1">👋</div>
            <span class="text-xs font-semibold text-white">終了</span>
        <% end %>
        <% else %>
        <%= link_to checkin_page_checkinout_records_path,
            class: "flex flex-col items-center bg-green-500 rounded-lg shadow p-3 sm:p-4 hover:shadow-md transition-all w-20 sm:w-24" do %>
            <div class="text-2xl sm:text-3xl mb-1">💼</div>
            <span class="text-xs font-semibold text-white">開始</span>
        <% end %>
        <% end %>

        <!-- グラフ -->
        <%= link_to analytics_moods_path,
            class: "flex flex-col items-center bg-white rounded-lg shadow p-3 sm:p-4 hover:shadow-md transition-all w-20 sm:w-24" do %>
        <div class="text-2xl sm:text-3xl mb-1">📊</div>
        <span class="text-xs font-semibold text-gray-700">グラフ</span>
        <% end %>
    </div>
</div>
