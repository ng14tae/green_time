<div class="hero min-h-screen bg-gradient-to-br from-orange-400 to-red-500">
    <div class="hero-content text-center text-white">
        <div class="max-w-md">
        <h1 class="text-5xl font-bold mb-8">Good Job!</h1>
        <p class="text-xl mb-8">今日もお疲れさまでした</p>

        <div class="work-summary mb-8 bg-white/20 rounded-lg p-6">
            <p class="text-lg">
            <%= Date.current.strftime("%Y年%m月%d日 (%a)") %></p><br>
            <p class="text-lg">現在時刻</p>
            <p class="text-3xl font-mono" id="current-time">
                <%= Time.current.strftime("%H:%M:%S") %></p><br>

            <p class="text-lg mb-2">チェックイン時刻</p>
            <p class="text-2xl font-mono mb-4"><%= @today_record.checkin_time.strftime("%H:%M") %></p>

            <p class="text-lg mb-2">勤務時間</p>
            <p class="text-3xl font-mono font-bold">
            <%= time_diff(@today_record.checkin_time, Time.current) %>
            </p>
        </div>

        <!-- ボタンエリア -->
        <div class="space-y-4">
        <!-- チェックアウトボタン -->
        <%= form_with url: checkout_checkinout_records_path, method: :post do |f| %>
            <%= f.submit "チェックアウト",
                class: "btn btn-lg px-16 py-4 text-2xl shadow-lg w-full bg-orange-800 hover:bg-orange-900 text-white border-0" %>
            <% end %>

            <!-- マイページボタン（アウトラインスタイル） -->
            <%= link_to mypage_checkinout_records_path,
                class: "btn btn-outline btn-lg px-16 py-4 text-2xl shadow-lg w-full block border-white text-white hover:bg-white/70 hover:border-white hover:text-orange-600 " do %>
            マイページを見る
            <% end %>
        </div>
        </div>
    </div>
</div>